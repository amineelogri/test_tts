var Kind = function() {};
Kind.extend = Backbone.Model.extend;
var Produit=Kind.extend({

  constructor: function(Id,Marque,Model,Etat,Desc){
    this.Id=Id,
    this.Id=Marque,
    this.Id=Model,
    this.Id=Etat,
    this.Id=Desc
  },


});
